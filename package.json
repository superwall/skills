{
  "name": "superwall-skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Superwall Agent Skills for Claude Code and the in-editor managed agent",
  "scripts": {
    "sync": "bun run scripts/sync-managed-agent.ts",
    "sync:dry": "DRY_RUN=1 bun run scripts/sync-managed-agent.ts",
    "sync:force": "FORCE_SYNC=1 bun run scripts/sync-managed-agent.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.2.9",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
